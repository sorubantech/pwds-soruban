# PSS 2.0 — Onboarding Build: Progress Tracker

**How this works:** this session (PM + prompt engineering) generates **one dev-session prompt at a time**. You run each prompt in a **separate development session**, report back the outcome, and only then do we generate the next prompt. This file is the single source of truth for where we are.

- **Task list:** `PSS-2.0-ONBOARDING-TASK-LIST.md`
- **Design:** `PSS-2.0-GTM-LEAD-ONBOARDING-AND-PRODUCT-ADMIN-APPROACH.md`, `PSS-2.0-PLANS-AND-ENTITLEMENTS-APPROACH.md`

---

## Prompt sequence (locked)

| # | Prompt file | Covers (task IDs) | Depends on | Blocking decision | Status |
|---|-------------|-------------------|-----------|-------------------|--------|
| **P-01** | `PSS-2.0-ONBOARDING-PROMPT-01-DATA-MODEL.md` | T-A1 provisioning run/step tables · T-A2 Company lifecycle columns · T-A3 template company shell | — | none | ✅ **DONE — build clean · migration + seed APPLIED 2026-07-28** |
| P-02 | `PSS-2.0-ONBOARDING-PROMPT-02-BILLING-ENTITLEMENTS.md` | T-A4 billing entities · T-A5 IEntitlementService + plan seed | P-01 | ✅ D-Q4 cleared | ✅ **DONE** (reported clean by dev session) |
| **P-02b** | `PSS-2.0-ONBOARDING-PROMPT-02B-MULTICURRENCY-PRICING.md` | multi-currency plan pricing — `billing.PlanPrice` price book + FX fallback · Subscription snapshot columns + `PaymentGatewayCode` · `IPlanPricingService` · prices seed | P-02 | ✅ D-Q9 cleared (price-book + FX; defer gateway) | ✅ **DONE — build clean, PM-verified** (Subscription snapshot built as `CurrencyId` FK, accepted — see P-03 block) |
| **P-03** | `PSS-2.0-ONBOARDING-PROMPT-03-PROVISIONING-ENGINE.md` | T-A6 ProvisionTenantCommand + steps 1–9 · T-A7 subdomain · T-A8 activation token · T-A9 PLATFORM_* · T-A10 abandon | P-01, P-02, **P-02b** | ✅ D-Q7 cleared | ✅ **DONE — build clean (0 err), PM-verified 2026-07-27** |
| **P-04** | `PSS-2.0-ONBOARDING-PROMPT-04-PROVISIONING-API-AND-MONITOR.md` | T-A11 provisioning GraphQL API · T-A12 O-03 Run Monitor screen · **account-activation endpoint** (carried from P-03) | P-03 | — | ✅ **DONE 2026-07-27 — build 0 err, tsc exit 0, PM-verified 2026-07-28** (shipped one blocking gap: welcome email had no activation link — **fixed in P-05**) |
| **P-05** | `PSS-2.0-ONBOARDING-PROMPT-05-LEAD-DEAL-WIZARD.md` | T-B1 `ops.Lead` + `ops.CommercialTerm` · T-B2/B3 S-01/S-02 · T-B4 O-01 wizard (calls P-04 `ProvisionTenant`) · T-B6 welcome email | P-03, P-04 | D-Q5 → **resolved-by-config** (threshold = platform setting, default 15%) | ✅ **DONE 2026-07-27 — build 0 err, tsc exit 0, PM-verified 2026-07-28** (also closed P-04's activation-link gap) |
| **P-05b** | `PSS-2.0-ONBOARDING-PROMPT-05B-LEAD-LIFECYCLE.md` | T-B7 lead lifecycle enforcement — server-governed `Lead.Status` transition guard · `WON` derived from `ApproveCommercialTerm` (unreachable manually) · create-time `WON` block · FE dropdown → lifecycle action buttons | P-05 | — | 🟢 **GENERATED 2026-07-28 — awaiting dev-session run** (no schema/migration/capability/mutation) |
| **P-05c** | `PSS-2.0-ONBOARDING-PROMPT-05C-PAYMENT-GATEWAY-PICKER.md` | T-B8 payment-gateway picker — deal form free-text `paymentGatewayCode` → closed `FormSelect` (`RAZORPAY`/`STRIPE` + "— Not decided —"), stays optional (blank→null) | P-05 | — | 🟢 **GENERATED 2026-07-28 — awaiting dev-session run** (FE only, no schema/migration/capability/mutation/seed) |
| **P-05d** | `PSS-2.0-ONBOARDING-PROMPT-05D-AUTOAPPROVE-TOGGLE.md` | T-B9 auto-approval master switch — platform-global BOOLEAN `PLATFORM_DEAL_AUTO_APPROVE_ENABLED` (default `true`); `SubmitCommercialTerm` `autoApproved` gated so OFF ⇒ every deal → `PENDING_APPROVAL`; `LeadHelper.GetAutoApproveEnabledAsync` reuses `GetPlatformSettingAsync` | P-05 | — | ✅ **BUILT 2026-07-28** — BE 2 edits applied, build 0 errors; seed row awaiting user apply |
| **P-05e** | `PSS-2.0-ONBOARDING-PROMPT-05E-DROP-WIZARD-DOCHEADER.md` | T-B10 drop `companyHeader`/`companyFooter` (Document header/footer) from O-01 wizard Step 3 — schema + form + request payload; redundant vs tenant-owned `Company` columns + template-driven receipts/pages/comms | P-05 | — | 🟢 **GENERATED 2026-07-28 — awaiting dev-session run** (FE only, 2 files; no BE/schema/migration/capability/mutation/seed; `Company` columns retained, handler defaults blank→company name) |
| **P-05f** | `PSS-2.0-ONBOARDING-PROMPT-05F-PROVISION-ROLE-INDEX-AND-RESUME.md` | T-B11 CRITICAL provisioning fix — Defect A: Role unique indexes `(RoleName/RoleCode/OrderBy, IsActive)` are global → Step 3 `SEED_ROLES` `roles_isactive` collision blocks every 2nd tenant; prepend `CompanyId`. Defect B: validator rejects resumable retry ("subdomain/code taken") → resume-aware `MustAsync` + shared `ProvisionIdempotency.KeyFor`. Index-only migration USER-OWNED; fix-forward (no rollback path) | P-03/P-04 | — | 🟢 **GENERATED 2026-07-28 — awaiting dev-session run** (BE: 1 config 3-index edit + validator + key helper; migration user-owned, dev proves compile only; no rollback/new column/capability/mutation/query/seed/FE) |
| **P-06** | `PSS-2.0-ONBOARDING-PROMPT-06-GOLIVE.md` | T-C1 O-04 go-live checklist (tenant `(core)`, BUSINESSADMIN) · T-C2 `CompleteGoLiveCommand` → `Status=ACTIVE` + `OnboardedOn` | P-05 | — | 🔵 **run reported 2026-07-27 — hand-back not pasted** |
| **P-07** | `PSS-2.0-ONBOARDING-PROMPT-07-TENANT-LIST.md` | T-A13 O-05 Tenant List `/ops/tenants` + read-only Tenant Detail `/ops/tenants/[companyId]` · header nav → O-03 monitor (control-plane, read-only MVP) | P-04 | — | ✅ **DONE 2026-07-28 — BE 0 CS err, tsc exit 0, hand-back written** (awaiting PM verify) |
| P-08 | *(not generated)* — public product-page lead-capture (EXTERNAL_PAGE → `ops.Lead`) | new GTM scope | P-05 | D-Q1 (do we sell self-service?) | ⬜ not generated |

**Combined dev-session runner (P-05 follow-ups):** the four outstanding P-05 patches — **P-05f (critical) + P-05b + P-05c + P-05e** — are bundled for a single dev session in `PSS-2.0-ONBOARDING-PROMPT-05-FOLLOWUP-COMBINED-RUN.md` (run 05f first — it carries the only migration, user-owned; the other three are order-free). This **supersedes** the earlier `PSS-2.0-ONBOARDING-PROMPT-05BC-COMBINED-RUN.md` (05b+05c only). Disjoint file touch-sets → no merge conflict; one combined BE+FE build evidence + hand-back. P-05d is **not** in the runner (BE already built; only its seed apply is pending).

**Phase 0 (infra: wildcard DNS + DNS-01 cert + three-host binding)** runs in parallel as an **ops/user task, not a code prompt.** It must be finished before P-03's provisioning engine is demoable end-to-end. Track it separately.

**Legend:** ⬜ not generated · 🟡 generated, awaiting dev run · 🔵 running in dev session · ✅ done + verified · 🔴 blocked

---

## Session log

### P-01 — Provisioning data model
- **Generated:** 2026-07-24
- **Prompt:** `PSS-2.0-ONBOARDING-PROMPT-01-DATA-MODEL.md`
- **Status:** 🟡 ready to run in a dev session
- **Verified 2026-07-24:** re-checked the prompt's EF anchors against the real codebase and corrected two inaccuracies before hand-off — (1) module context is a **partial class of `ApplicationDbContext`** (`public partial class ApplicationDbContext : IOpsDbContext`, DbSets via `Set<T>()`) + interface added to `IApplicationDbContext`, **not** a standalone `OpsDbContext` with its own DI registration; (2) mapping split — table/schema via `[Table(Schema="ops")]` annotation, everything else (keys/FK/indexes/jsonb) via `IEntityTypeConfiguration<T>` classes in a new `Configurations/OpsConfigurations/` folder, auto-discovered by the existing `ApplyConfigurationsFromAssembly` (no manual registration). jsonb mapped as `string` + `.HasColumnType("jsonb")`, not `JsonDocument`. Anchors (Entity base, Company.cs, GrantFundReceiptConfiguration.cs, sql-scripts-dyanmic) all confirmed real.
- **Expected hand-back:** entities + EF config compile clean; **migration spec** for the user to author/run; template-company seed script; a short note of what was built.
- **Result (2026-07-24): ✅ DONE, build clean.** Full `PeopleServe.sln` build 0 errors (652 pre-existing warnings, unchanged). Delivered exactly to scope: `ops.TenantProvisioningRuns` + `ops.TenantProvisioningRunSteps` entities, `OpsConfigurations/` config classes (jsonb-as-string, unique `IdempotencyKey`, unique `(RunId,StepNumber)`, wired FK `CompanyId → app.Companies`), partial-class facet (`IOpsDbContext` + `public partial class ApplicationDbContext : IOpsDbContext`, added to `IApplicationDbContext`, no new DbContext / no DI change), `app.Companies` +4 columns (`Status`, `IsInternal`, `OnboardedOn`, `SourceLeadId`).
- **Deliverables:** migration spec `PSS-2.0-ONBOARDING-P01-MIGRATION-SPEC.md` · seed `sql-scripts-dyanmic/ops-template-company-seed.sql` (idempotent `__TEMPLATE__` shell) · hand-back `PSS-2.0-ONBOARDING-P01-HANDBACK.md`.
- **⚠ Pending user action:** author + run the migration (per spec) and apply the seed. Migrations stay user-owned; nothing was run. Later prompts *compile* without this, but the engine can't *run* until the migration is applied.
- **Carry-forward deviations (feed into later prompts):**
  1. **Target framework is `net10.0`**, not .NET 8 — update the header of P-02..P-06 prompts to say net10.0.
  2. **`Company.Status` default = `ACTIVE`** for existing rows (backfill `UPDATE` is in the migration spec); column stays nullable/additive-safe.
  3. **`ops.TenantProvisioningRun` inherits the global tenant query filter** (it has a `CompanyId`). Harmless for a SuperAdmin control-plane read, but **P-03/P-04 authors must query it in an ops/SuperAdmin context** (`IgnoreQueryFilters()` or the platform read path) — otherwise a run whose `CompanyId` isn't the caller's tenant vanishes. **Add this note to P-03 and P-04 prompts.**
  4. FK deferrals confirmed for `LeadId`, `CommercialTermId`, `SourceLeadId`, `InitiatedByUserId` (their tables land in later prompts).
  5. `Base.Domain.Models.OpsModels` added to `Base.Infrastructure`'s `GlobalUsing.cs`.

---

### P-02 — Billing schema + entitlement service + plan seed
- **Generated:** 2026-07-24
- **Prompt:** `PSS-2.0-ONBOARDING-PROMPT-02-BILLING-ENTITLEMENTS.md`
- **Status:** 🟡 ready to run in a dev session
- **Unblocked by:** D-Q4 DECIDED (module→plan matrix in `PSS-2.0-ONBOARDING-DQ4-MODULE-PLAN-MAP.md`).
- **Scope:** 6 `billing` entities (Plan/PlanEntitlement/PlanQuota/Subscription/SubscriptionOverride/UsageCounter) via the P-01 partial-class + `BillingConfigurations/` pattern · `IEntitlementService` (resolve override-then-plan, cache+invalidate, fail-closed) · 2 seeds (plan catalog per DECIDED matrix + backfill existing companies to CUSTOM). `Subscription.CommercialTermId` = no FK yet (P-05).
- **Expected hand-back:** build clean; migration spec; two seed scripts; verified real `auth.Modules` codes.
- **Result (2026-07-24): ✅ DONE** — dev session reported P-02 complete/clean. (Detailed hand-back not relayed to PM session; if there were deviations or module-code mismatches, point me at the P-02 hand-back file and I'll fold them into P-03.)
- **⚠ Pending user action:** author + run the billing migration + apply both seeds (plan catalog + backfill), same as P-01.

---

### P-02b — Multi-currency plan pricing (price book + FX fallback)
- **Generated:** 2026-07-24
- **Prompt:** `PSS-2.0-ONBOARDING-PROMPT-02B-MULTICURRENCY-PRICING.md`
- **Status:** 🟡 ready to run in a dev session — **run before P-03** (P-03 step 2 consumes `IPlanPricingService`).
- **Why it exists:** the product is global with multiple payment gateways; a plan carries only one base `Price`/`Currency`, so a tenant must be able to buy in *any* currency. **D-Q9 DECIDED** (see brief below): price book + FX fallback; defer the gateway integration, capture a `PaymentGatewayCode` string now.
- **Scope (small amendment that folds into the not-yet-applied P-02 billing migration — one migration, not two):**
  1. New `billing.PlanPrice` entity (`(PlanId, CurrencyId, BillingCycle)` unique; hand-set curated prices). **Catalog currency = `CurrencyId` FK → `com.Currencies` (Restrict), NOT an ISO string** (refined 2026-07-24 — see below). `Plan.Price/BillingCycle` **kept** as the base anchor + FX source (not dropped); **`Plan.Currency` string → `Plan.CurrencyId` FK** (P-02 amendment — catalog/seed updated to resolve `CurrencyCode`→`CurrencyId`).
  2. `Subscription` +4 additive **nullable** snapshot columns — `Currency`/`Amount`/`BillingCycle`/`PaymentGatewayCode`. **⚠ As-built deviation (accepted):** the currency column shipped as **`CurrencyId int` FK → `com.Currencies` + `Currency` nav**, NOT the ISO string this prompt specified. Accepted at P-03 verify (2026-07-27) — `com.Currencies` is an immutable master (not a mutable price/rate row), the price VALUE (`Amount`) is still snapshotted, so the snapshot rule holds in spirit; and it's consistent with the `CurrencyId`-keyed catalog. `Amount`/`BillingCycle`/`PaymentGatewayCode` unchanged (no FK). **P-04 wizard DTO must pass `CurrencyId int`.**
  3. `IPlanPricingService.ResolveAsync(planCode, **int currencyId**, billingCycle)` → `PriceResolution(Amount, **CurrencyId, CurrencyCode**, BillingCycle, Source)` where Source = FREE→BOOK→FX→**null (unsellable, fail-closed)**; catalog read with `IgnoreQueryFilters()` + explicit `IsDeleted != true`; FX via `IFxRateService` direct-pair (both from/to are already int IDs — no ISO→Id lookup); snapshot the resolved VALUE (`CurrencyCode` string), never an FK.
  4. Seed `sql-scripts-dyanmic/billing-plan-prices-seed.sql` (idempotent; `CurrencyId` resolved from `CurrencyCode` subquery, skip currencies absent from `com.Currencies`; INR/USD/EUR/GBP/AUD/SGD/AED/CAD × PLAN_50K/PLAN_100K × both cycles; FREE special-cased; CUSTOM no book).
- **Deliverable spec:** `PSS-2.0-ONBOARDING-P02B-MIGRATION-SPEC.md` (amendment — "fold into the P-02 billing migration if not yet run").
- **🔧 Refined 2026-07-24 (CurrencyId FK decision):** after the FK-vs-hardcoded audit, catalog-side currency was pinned to a **`CurrencyId` FK → `com.Currencies`** (the real currency master already used by `IFxRateService`), not an ISO string — applied to **both `Plan.CurrencyId`** (P-02 amendment) **and `PlanPrice.CurrencyId`**. Rationale: referential integrity, ISO→CurrencyId is now free for the FX fallback (both sides int-keyed), and consistency with the rest of the app. P-02b §1/§1b/§3/§4/§5 + P-03 step 2 DTO (`CurrencyId int`) all rewritten to match.
- **🔧 Superseded 2026-07-27 (Subscription snapshot too):** the prompt still asked for `Subscription.Currency` as an ISO string exception, but the dev built it as **`CurrencyId int` FK → `com.Currencies` + nav** (with `Amount`/`BillingCycle`/`PaymentGatewayCode` snapshots). **Accepted at P-03 verify** — an immutable currency master is not a mutable price/rate row, and `Amount` still snapshots the VALUE, so the snapshot rule is intact; it's also more consistent with the now-`CurrencyId`-keyed catalog. The whole billing stack (catalog + snapshot) is therefore uniformly `CurrencyId`-keyed. Prompt text (P-02b §2 + P-03 step 2) is now historical — HANDBACK + this tracker are authoritative.
- **⚠ Pending user action:** if P-02's migration is **not yet run**, add these columns/table into it (incl. the `Plan.Currency`→`Plan.CurrencyId` change); if it **is** run, author a small follow-up migration with the two-step column swap (add `CurrencyId`, backfill from `Currency`, drop `Currency`). Then apply the prices seed. Requires `com.Currencies` to hold the seeded ISO codes first.

---

### P-03 — Tenant provisioning engine (`ProvisionTenantCommand` + 9 steps)
- **Generated:** 2026-07-24
- **Prompt:** `PSS-2.0-ONBOARDING-PROMPT-03-PROVISIONING-ENGINE.md`
- **Status:** ✅ **DONE — build clean, PM-verified 2026-07-27**
- **Unblocked by:** D-Q7 DECIDED (role matrix in `PSS-2.0-ONBOARDING-DQ7-PLATFORM-ROLES-MAP.md`).
- **Result (2026-07-27): ✅ DONE.** `dotnet build Base.Application` → **0 errors** (575 pre-existing warnings). Hand-back `PSS-2.0-ONBOARDING-P03-HANDBACK.md`. **PM verified all 5 deliverables against real source** (not just the hand-back): `ProvisionTenant.cs` (command+validator+9-step engine, each step own-tx via `CreateExecutionStrategy`+own `RunStep` row, resume skips SUCCEEDED, idempotency key `LEAD:{id}|CODE:{code}`, `IgnoreQueryFilters()`+explicit `CompanyId` throughout, Company stays `PROVISIONING`, fail-closed currency, no-password activation token, `ModuleFeatureMap` bridge for step 4), `AbandonProvisioningRun.cs` (pre-step-8 guard — hard-blocks once a `UserRole` exists), `ops-platform-rbac-seed.sql` (idempotent `NOT EXISTS`, 1 module/5 menus/10 caps/5 roles/20 grants, provision = IMPLEMENTATION+ADMIN). **One material deviation, accepted:** step 2 snapshots `Subscription.CurrencyId` (int FK → `com.Currencies`) instead of the ISO string the prompt specified — see P-02b block above; the whole billing stack is now uniformly `CurrencyId`-keyed.
- **⚠ Pending user action:** (a) apply `sql-scripts-dyanmic/ops-platform-rbac-seed.sql` (after the auth schema exists); (b) *optionally* author a migration for a filtered UNIQUE index on `app."Companies"("Subdomain")` — the validator enforces uniqueness at app-layer regardless, so this is a hardening nicety, not a blocker; (c) bind real internal staff users to the `PLATFORM_*` roles (operational, tracked for P-04/A-14). No password is ever generated/emailed/displayed — activation token only.
- **Deferred to P-04 (documented in hand-back §6):** the token-consuming set-password/activation endpoint; the GraphQL mutation/resolver that calls these commands + supplies HTTP/user context (populates `InitiatedByUserId`); O-03 Run Monitor screen. `ops.Lead`/`ops.CommercialTerm` FKs stay nullable ints until P-05.
- **Scope:** `ProvisionTenantCommand` handler running the **9 idempotent, resumable steps** (CREATE_COMPANY → … → SEND_WELCOME), each in its own transaction + own `ops.TenantProvisioningRunStep` row · T-A7 `Company.Subdomain` (the only additive column) + DNS-label validator + reserved blocklist · T-A8 no-password single-use hashed **activation token** · T-A9 `PLATFORM_*` seed (5 global roles + 10 caps + `(master)` menus + role bundles, all `IsSystem=true`) · T-A10 abandon-cleanup (pre-step-8 only; suspend-not-delete once an admin exists).
- **Key correctness rules baked into the prompt:** target `net10.0`; handler runs in **SuperAdmin/null-tenant context** — every cross-tenant read uses `IgnoreQueryFilters()`, every written tenant row sets `CompanyId` **explicitly** (the global filter is read-only, never stamps writes); tenant roles/masterdata/settings **clone from `__TEMPLATE__`**; step 2 reads plan from the **request payload** (not a CommercialTerm FK — that's P-05, `Subscription.CommercialTermId` stays null); step 8 **diverges from `SendUserInvite`'s temp-password path** → mints a `PasswordReset`-style token, emails a link, no password ever; step 9 guards the `Lead.ConvertedCompanyId` stamp (ops.Lead absent until P-05); Company stays `PROVISIONING` (go-live→ACTIVE is P-06).
- **Two flagged decisions the dev finalizes against live code (with my recommendation in-prompt):** (a) PLATFORM_* seed — full menu+cap+bundle *[recommended]* vs caps+roles-only with bundling deferred to P-04 if menu-seeding drags in heavy infra; (b) whether the token-consuming "set password" endpoint exists or is a P-04 follow-up.
- **Expected hand-back:** build clean; 9-step engine idempotent+resumable; subdomain column migration spec; PLATFORM_* seed script (which path taken); abandon guard; list of any property/table names that differed.
- **⚠ Depends on P-01/P-02/P-02b migrations being applicable** — the engine *compiles* without them but can't *run* end-to-end until the ops + billing (incl. P-02b price book/snapshot columns) migrations are run and the `__TEMPLATE__` company is populated (an ops step, per P-01). **Phase 0 infra** (wildcard DNS + cert + host binding) is still needed before a run is demoable.
- **Revised 2026-07-24 (grounded against real entities + multi-currency):** nine targeted edits after reading `Company.cs` / `Subscription.cs` / `Plan.cs` / P-02 hand-back —
  1. **T-A7 corrected — `Company.Subdomain` (and `CustomDomain`) ALREADY EXIST** (Screen #119). P-03 adds **no column and no column-add migration**; it only adds a filtered unique index on `Subdomain` **if one is actually missing**, plus the DNS-label validator + reserved blocklist. (The earlier "add Subdomain if absent" instruction was factually wrong and is removed.)
  2. **Real required (NOT-NULL) columns now enumerated** so every provisioned row is complete — Step 1 Company: `CompanyCode`, `CompanyName`, `CompanyHeader`, `CompanyFooter`, `Address`, `CountryId` (real FK), `IsInternal=false`, `Status='PROVISIONING'`. Step 2 Subscription + Run/RunStep NOT-NULLs (incl. jsonb-as-string `RequestPayloadJson`) listed. Command DTO expanded to carry them (CountryId, Address, CompanyHeader/Footer + rule).
  3. **Step 2 CREATE_SUBSCRIPTION now multi-currency** — calls `IPlanPricingService.ResolveAsync(PlanCode, request.Currency, request.BillingCycle)`; **null ⇒ fail the step cleanly** (PAUSED_ON_ERROR, "no price for {Plan} in {Currency}") — no silent base-currency fallback; snapshots Currency/Amount/BillingCycle/PaymentGatewayCode as VALUE.
  4. **FeatureCode ↔ auth.Module mapping layer** spelled out for step 4 (coarse Guid-keyed `auth.Modules` vs fine `MODULE:*` FeatureCodes — a `static readonly Dictionary<ModuleCode, FeatureCode[]>`; grant a coarse RoleModule only if ≥1 mapped FeatureCode is entitled; SETTING/GENERAL/ACCESSCONTROL always-on).
  - Definition of Done rewritten to 9 items reflecting all of the above.

---

### P-04 — Provisioning GraphQL API + O-03 Run Monitor + account-activation endpoint
- **Generated:** 2026-07-27
- **Prompt:** `PSS-2.0-ONBOARDING-PROMPT-04-PROVISIONING-API-AND-MONITOR.md`
- **Status:** ✅ **DONE 2026-07-27 — build 0 err, tsc exit 0 — PM-verified against hand-back 2026-07-28.** Prompt file untouched since run.
- **Result:** 3 mutations (`provisionTenant`/`resume`/`abandon`, acting user from JWT `UserId` claim; `InitiatedByUserId` now stamped — closes P-03 gap #3), O-03 Run Monitor screen (list + 9-step timeline, capability-gated Resume/Abandon), anonymous `ActivateAccount` command/query/mutation + public `/activate` page (passwordless, token burned on use). New `usePlatformCapabilities` hook (shared `useCapablities` can't serve `PLATFORM_*`).
- **⚠ Blocking gap flagged (P-04 §2.1):** welcome email carried **no activation link** — invited admin couldn't reach `/activate`. **RESOLVED in P-05** (new `TENANT_ADMIN_ACTIVATION` template + `BuildActivationUrlAsync`).
- **Other flagged items:** list status-filter is client-side only (`GridFeatureRequest` has no status predicate); seeded `PLATFORM_TENANTS` menu URL `/ops/tenants` vs monitor at `/ops/tenants/provisioning-runs` (tenant list not built); `checkSubdomain` not built (flagged to P-05, still open).
- **Scope (3 parts):**
  1. **T-A11 — provisioning GraphQL API.** `TenantProvisioningMutations : IMutations` exposing **ProvisionTenant** / **ResumeProvisioningRun** / **AbandonProvisioningRun**, each resolving the acting user from the `UserId` claim (`_httpContextAccessor.HttpContext?.User.FindFirst("UserId")`). **Fixes the P-03 gap:** threads that userId into the command so `TenantProvisioningRun.InitiatedByUserId` is finally populated (P-03 left it null — the *one* permitted wiring edit to `ProvisionTenant.cs`). New thin `ResumeProvisioningRunCommand(int RunId)` loads the run with `IgnoreQueryFilters()`, refuses SUCCEEDED/ABANDONED, and **delegates to `ProvisionTenantCommand`** (engine skips SUCCEEDED steps) — does not reimplement step logic.
  2. **T-A12 — O-03 Run Monitor.** `TenantProvisioningQueries : IQueries` — `GetProvisioningRuns` (paginated list) + `GetProvisioningRunById` (steps eager-loaded, ordered), both `IgnoreQueryFilters()` + `IsDeleted != true`, gated `PLATFORM_TENANT_VIEW`. Control-plane `(master)` screen: run list with status chip + steps-succeeded/9 progress; detail = 9-step vertical timeline surfacing the failed step's `ErrorMessage`; state-correct capability-gated **Resume** (`PLATFORM_TENANT_PROVISION`, only when PAUSED_ON_ERROR) / **Abandon** (`PLATFORM_TENANT_SUSPEND`, pre-step-8) actions that refetch on success.
  3. **Account-activation endpoint (deferred from P-03).** Dedicated `ActivateAccountCommand` + `ActivateAccountTokenValidationQuery` + **anonymous** `ActivateAccount` mutation — consumes the P-03 activation token: single-use + **72h expiry (no 48h cap)**, **null-guards** the `PasswordReset` lookup (avoids the ResetTokenValidations null-ref), clears `IsPendingInvitation`/`MustChangePassword` + sets `IsActive`, **never** generates/returns/logs a password. Public activation page consuming it (reuse/clone the existing reset-password page), matching the welcome-email link.
- **Grounded against real source before authoring:** mutation-wrap + acting-user pattern (`AuthendicationMutations.cs` `SwitchCompany` line ~279), query-exposure pattern (`OrganizationBankAccountQueries.cs`), token-consume flow (`ResetPasswords.cs` + `ResetTokenValidations.cs` — noted its 48h cap + null-ref), the two `ops` entities (exact field names), `AbandonProvisioningRun.cs` + `ProvisionTenant.cs` signatures.
- **Key correctness rules baked in:** target `net10.0`; **no schema change / no migration** (adds mutations/queries/one command-query pair/screen only — if a column looks missing, produce a migration spec instead); `ops` run/step reads all `IgnoreQueryFilters()` + explicit `IsDeleted != true` (platform null-tenant context); wizard DTO passes **`CurrencyId int`** (per P-02b/P-03 as-built, not ISO string); no password ever produced.
- **Expected hand-back:** build clean (or left-to-user); three mutations wired + `InitiatedByUserId` now populated (+ the exact one-line `ProvisionTenant.cs` edit); resume delegates-not-reimplements; two monitor queries + O-03 screen; activation command/query/mutation + public page (reused reset page? Y/N); whether `InitiatedByUserId` went on the command or the DTO; the welcome-email route matched (or mismatch found); any property/table/route names that differed.
- **Out of scope (later prompts):** engine step-logic changes · `ops.Lead`/`ops.CommercialTerm` + S-01/S-02/O-01 wizard (P-05) · go-live/`Company.Status → ACTIVE` (P-06) · impersonation/suspend-live-tenant · re-seeding PLATFORM_* (T-A9 already did it).

### P-05 — Lead + Commercial Term + O-01 Provisioning Wizard + welcome email (Phase B, assisted onboarding)
- **Generated:** 2026-07-27
- **Prompt:** `PSS-2.0-ONBOARDING-PROMPT-05-LEAD-DEAL-WIZARD.md`
- **Status:** ✅ **DONE 2026-07-27 — build 0 err, tsc exit 0 — PM-verified against hand-back 2026-07-28.** Prompt file untouched since run.
- **Result:** all 5 deliverables — `ops.Lead` + `ops.CommercialTerm` entities/config/DTOs/CRUD; S-01 `/ops/leads`; S-02 `/ops/deals` (one screen, unscoped = approval queue, `?leadId=` = per-prospect); O-01 wizard `/ops/onboarding/provision` (4 steps → `provisionTenant` → run monitor); welcome email now links to real `/{lang}/activate?token=…` on tenant subdomain. Threshold read from `sett.OrganizationSettings` (`CompanyId IS NULL`, `PLATFORM_DISCOUNT_APPROVAL_THRESHOLD_PCT`, default 15, helper `LeadHelper`). `Lead.ConvertedCompanyId` stamped in step 9 (idempotent); `Company.SourceLeadId` in step 1.
- **Key as-built deviations (test-relevant):** (1) email template = new **`TENANT_ADMIN_ACTIVATION`** not `USER_WELCOME_INVITE` (shared template can't suppress temp-password block); (2) **plan picker = static FE list** `FREE/PLAN_50K/PLAN_100K/CUSTOM` — `billing.Plans` has NO GraphQL read surface yet; (3) **no `checkSubdomain`** — uniqueness fails only after Provision click; (4) 4 deferred FKs left as nullable ints (raw-SQL `ALTER` in spec §4); (5) no new capabilities — reuses P-04 set (DQ7's `PLATFORM_LEAD_MANAGE`/`_CONVERT`/`DEAL_VIEW`/`DEAL_MANAGE`/`ONBOARD_RUN` NOT created).
- **⚠ Pending user action (apply checklist §10):** author+run `PSS-2.0-ONBOARDING-P05-MIGRATION-SPEC.md` (2 ops tables + 4 FKs); apply `ops-platform-rbac-seed.sql` (done) + **`ops-lead-deal-seed.sql`** (new); set `PLATFORM_ACTIVATION_URL_TEMPLATE.CurrentValue` to real host pattern before any real run.
- **Depends on P-04 as-built** — the wizard calls the P-04 `ProvisionTenant` mutation and the email links to the P-04 `/activate` route. **⚠ P-04 hand-back was not pasted before generation** — prompt instructs the dev to reconcile the wizard payload + activation route against the *real* P-04 output; if P-04 deviated from spec, PM to patch the anchors on hand-back.
- **Scope (5 parts):**
  1. **T-B1 — `ops.Lead` (thin CRM) + `ops.CommercialTerm` (deal + discount approval)** entities + EF config + DTOs + CRUD commands/queries. `CommercialTerm` computes List/Discount/Net **server-side** from `IPlanPricingService` (fail-closed on no price). `CurrencyId int` FK. Both are platform rows (`CompanyId` null) → all reads `IgnoreQueryFilters()` + `IsDeleted != true`.
  2. **T-B2 — S-01 Lead screen** (thin, `(master)` surface, `PLATFORM_LEAD_*`). No full-CRM surface.
  3. **T-B3 — S-02 Commercial Term screen** + discount-approval workflow (submit → auto-approve-under-threshold / else PENDING_APPROVAL; approve/reject gated `PLATFORM_DEAL_APPROVE` = Finance+Admin per DQ7).
  4. **T-B4 — O-01 Provisioning Wizard** — WON lead + APPROVED term → payload matching `ProvisionTenantRequestDto` (`CurrencyId int`, `Mode="ASSISTED"`, `LeadId`+`CommercialTermId`) → **calls P-04 `ProvisionTenant`** → routes to O-03 monitor. Stamps `Lead.ConvertedCompanyId` once (server-side — dev verifies where).
  5. **T-B6 — Welcome email** — finalize `USER_WELCOME_INVITE` body + activation link to the real P-04 route; token only, no password.
- **D-Q5 resolved-by-config:** discount-approval threshold modeled as a **configurable platform setting** (`PLATFORM_DISCOUNT_APPROVAL_THRESHOLD_PCT`, **default 15%**), seeded + tunable — approval *mechanism* fully built, number is not hardcoded. **User: change the seeded default if 15% is wrong.**
- **Schema:** two new `ops` tables + activation of four existing nullable-int FKs (`TenantProvisioningRun.LeadId/CommercialTermId`, `Company.SourceLeadId`, `Subscription.CommercialTermId`) → **migration spec for the user** (no migration authored/run). New seed `ops-lead-deal-rbac-seed.sql` (Lead/Deal/Wizard menus+caps+grants per DQ7 + threshold default) — I write, user applies.
- **Expected hand-back:** build clean (or left-to-user); entities + CRUD + approval workflow + where the threshold is read from; migration spec for 2 tables + 4 FKs; seed + the capabilities/grants created (reconciled vs DQ7); S-01/S-02/O-01 done; the **exact `ProvisionTenant` input sent** vs `ProvisionTenantRequestDto` as-built; where `Lead.ConvertedCompanyId` is stamped; the activation route the email links to; any property/table/route/capability name that differed.
- **Out of scope:** go-live/status flip (P-06) · changes to P-03 engine or P-04 API · full CRM · gateway integration · self-service public signup.

### P-05b — Lead lifecycle enforcement (T-B7, follow-up patch to P-05)
- **Generated:** 2026-07-28
- **Prompt:** `PSS-2.0-ONBOARDING-PROMPT-05B-LEAD-LIFECYCLE.md`
- **Status:** 🟢 **GENERATED 2026-07-28 — awaiting dev-session run.**
- **Why:** P-05 shipped `Lead.Status` as a *vocabulary-only* guard — `CreateLead`/`UpdateLead` accept any `AllowedStatuses` value in any order, and the FE binds a free `FormSelect` to `LEAD_STATUS_OPTIONS` (incl. `WON`). So the lifecycle is effectively a manual dropdown: an edit can jump `NEW→WON`, skip `QUALIFIED`, or go backwards. Intended design is an **ordered, server-enforced** progression where **`WON` is earned by an approved deal**, never hand-picked.
- **Scope (4 BE edits + FE swap, all in existing files — no schema/migration/capability/mutation):**
  1. `LeadHelper.cs` — add `_allowedStatusTransitions` table + `IsAllowedManualStatusTransition(from, to)` (`NEW→{QUALIFIED,LOST}`, `QUALIFIED→{LOST}`, `WON→{}` terminal, `LOST→{NEW}` reopen; same-state always OK; `WON` never a legal manual target).
  2. `UpdateLead.cs` — non-converted branch throws `BadRequestException` on illegal transition before `dto.Adapt`.
  3. `ApproveCommercialTerm.cs` — on approve, auto-advance parent lead (`entity.LeadId`, `IgnoreQueryFilters` + `IsDeleted != true`, only if not-converted & `NEW`/`QUALIFIED`) to `WON` in the **same** `SaveChangesAsync`; plus a pre-condition guard blocking approval of a deal on a `LOST` lead.
  4. `CreateLead.cs` — validator blocks `WON` at creation (still allows `LOST`).
  - **FE:** remove the manual status `FormSelect`; add lifecycle action buttons (Qualify / Mark Lost + reason / Reopen) reusing `UPDATE_LEAD`; `WON` is a read-only chip; drop `WON` from selectable options.
- **Design decision (recommended #2, user-approved "ok sure"):** `WON` derived from `CommercialTerm` approval; `QUALIFIED`/`LOST` stay manual. Idempotent auto-WON (re-approval / already-WON / converted = no-op).
- **Expected hand-back:** BE 0 CS err; `npx tsc --noEmit --incremental false` exit 0; confirm the 4 edits, the exact rejected transitions, create rejects `WON`, FE dropdown gone → action buttons, and where the buttons live (list row vs detail header).

### P-05c — Payment-gateway picker (T-B8, follow-up patch to P-05)
- **Generated:** 2026-07-28
- **Prompt:** `PSS-2.0-ONBOARDING-PROMPT-05C-PAYMENT-GATEWAY-PICKER.md`
- **Status:** 🟢 **GENERATED 2026-07-28 — awaiting dev-session run.**
- **Why:** the deal form renders `CommercialTerm.PaymentGatewayCode` as a **free-text `FormInput`** (placeholder "Optional — e.g. RAZORPAY"). That code carries onto `Subscription.PaymentGatewayCode` and drives collection routing, so an unconstrained string (`razorpay`/`RazorPay`/typos) is a latent data-quality hole.
- **Scope (2 FE edits — no BE/schema/migration/capability/mutation/query/seed):**
  1. `CommercialTermDto.ts` — add `PAYMENT_GATEWAY_OPTIONS` constant (`""`→"— Not decided —", `RAZORPAY`, `STRIPE`), mirroring the existing `BILLING_CYCLE_OPTIONS` shape.
  2. `deal-form-dialog.tsx` — swap the gateway `FormInput` for a `FormSelect` bound to it (already imports `FormSelect`); field stays **optional** (blank option `""` → existing submit line coalesces to `null`; `resetTermToForm` maps `null→""` = the blank option). Schema untouched (`.optional().nullable()` already accepts `""`).
- **Design decision:** hard-coded FE list of the **two codes the backend actually names**, not a server-configurable `PLATFORM_PAYMENT_GATEWAYS` setting. Rationale: gateways aren't integrated for collection yet, and a configurable list needs a BE read-path that doesn't exist — deferred (left a `// TODO`) until a real gateway integration lands. Answers the user's "give dropdown" ask without adding un-routable options.
- **Expected hand-back:** `npx tsc --noEmit --incremental false` exit 0; gateway is a dropdown of Razorpay/Stripe + "— Not decided —"; still optional, saves `null` when blank; editing a deal with an existing code pre-selects it; no BE/schema/seed touched.

### P-05d — Auto-approval master switch (T-B9, follow-up patch to P-05)
- **Generated:** 2026-07-28
- **Prompt:** `PSS-2.0-ONBOARDING-PROMPT-05D-AUTOAPPROVE-TOGGLE.md`
- **Seed:** `sql-scripts-dyanmic/ops-deal-autoapprove-toggle-seed.sql` (written — user applies after `ops-lead-deal-seed.sql`)
- **Status:** ✅ **BUILT 2026-07-28** — both BE edits applied, `dotnet build Base.API.csproj -c Debug` → **0 errors** (redirected `BaseOutputPath`; the in-place build only failed on MSB3021/MSB3027 DLL copy-locks from a running `Base.API`, no CS errors). Seed unchanged — **user still applies** `ops-deal-autoapprove-toggle-seed.sql`.
  - **Name deviation flagged:** §③ assumed `ApplicationDbContext`; the real sibling signature (`GetDiscountApprovalThresholdAsync`, `GetPlatformSettingAsync`) uses **`IApplicationDbContext`** — used that, no new context type introduced.
- **Why:** auto-approval was governed by only the *threshold percent* — there was no way to disable it entirely (even threshold `0` still auto-approves a 0%-discount deal). User asked for an easy ON/OFF master switch separate from the threshold.
- **Scope (BE 2 edits + 1 seed row — no schema/migration/capability/mutation/query/FE):**
  1. `LeadHelper.cs` — add `AUTO_APPROVE_ENABLED_PARAM_CODE` + `DefaultAutoApproveEnabled = true` + `GetAutoApproveEnabledAsync` (reuses the existing `GetPlatformSettingAsync`; safe-default TRUE on absent/blank/unparseable).
  2. `SubmitCommercialTerm.cs` — one line: `autoApproved = autoApproveEnabled && DiscountPercent <= threshold`. Everything downstream (APPROVED/PENDING branch, result DTO, FE toast keyed on `AutoApproved`) is unchanged.
- **Semantics:** `PLATFORM_DEAL_AUTO_APPROVE_ENABLED` BOOLEAN, platform-global, default `true`. **ON** = today's behaviour; **OFF** = every submitted deal → `PENDING_APPROVAL` regardless of discount (even 0%). DB-edited today (no platform-settings UI yet); the seed header documents the `UPDATE … CurrentValue='false'` one-liner.
- **Expected hand-back:** `dotnet build` 0 CS errors; with setting `'false'` a 0%-discount deal lands PENDING (not APPROVED); with `'true'`/absent, within-threshold still auto-approves; flag any name that differed.

### P-06 — O-04 Go-Live Checklist + `Company.Status → ACTIVE` (Phase C, go-live handoff · FINAL phase)
- **Generated:** 2026-07-27
- **Prompt:** `PSS-2.0-ONBOARDING-PROMPT-06-GOLIVE.md`
- **Status:** 🔵 run reported by user 2026-07-27 — **hand-back not pasted** (mark ✅ once pasted / spot-checked)
- **Two subtleties baked in (both easy to get wrong):**
  1. **Flip happens on checklist completion, NOT on provision.** P-03 deliberately created the Company `PROVISIONING` and never flipped it; §6.5 orders it `provisioned → activate → checklist → ACTIVE`. (§6.6.1 step 9's "Company ACTIVE" is the *self-service* future path — does not apply.)
  2. **O-04 is on the TENANT domain (`(core)`), gated BUSINESSADMIN** — customer-facing first-run, **not** the `(master)` control-plane / `PLATFORM_*` of P-04/P-05. So **no `IgnoreQueryFilters()`** here (normal tenant context is exactly what's wanted).
- **Scope (2 parts):**
  1. **T-C1 — O-04 checklist** (`CONFIG`/settings-page style): §6.5 items — branding uploaded · ≥1 additional user · gateway/email configured *(only if in plan, `IEntitlementService`)* · contacts imported-or-skipped · one test donation — each **derived server-side** via a `GetGoLiveChecklist` query returning per-item state + `canGoLive` + current Status. Progress indicator + **Go Live** button gated on required items. Contacts **Skip** → `GOLIVE_CONTACTS_SKIPPED` KV flag.
  2. **T-C2 — `CompleteGoLiveCommand`** (`CompleteGoLive` mutation, BUSINESSADMIN): **re-derives** required items server-side, flips `Status="ACTIVE"` + `OnboardedOn=UtcNow`, **idempotent**, refuses on unmet/`SUSPENDED`, audits `GO_LIVE`. No email (CS handoff is manual).
- **Zero-schema target:** skip-state via `OrganizationSetting` KV (**no new tables**); only *possible* schema is `Company.OnboardedOn`/`IsInternal` **if T-A2 wasn't applied** → migration spec for the user, else none. No migration authored/run.
- **D-Q8 (target time-to-live-tenant) is NOT a blocker** — it's a reporting SLA, not a code gate; P-06 honors `IsInternal` but builds no metrics.
- **Expected hand-back:** build clean (or left-to-user); whether `OnboardedOn`/`IsInternal` already existed (or spec handed over); the exact checklist items + how each is derived + which were plan-conditional; flip is idempotent + server-re-derived; confirmed on tenant `(core)`/BUSINESSADMIN; any property/table/setting-ParamCode/route name that differed.
- **Closes the loop:** WON lead → provisioned tenant → activated admin → **ACTIVE tenant**. Last onboarding phase.

### P-07 — O-05 Tenant List (`/ops/tenants`) + read-only Tenant Detail (control-plane, Phase A.4)
- **Generated:** 2026-07-28
- **Prompt:** `PSS-2.0-ONBOARDING-PROMPT-07-TENANT-LIST.md`
- **Status:** ✅ **DONE 2026-07-28 — hand-back `PSS-2.0-ONBOARDING-P07-HANDBACK.md`.** BE 0 `CS` errors (652 pre-existing warnings); FE `npx tsc --noEmit --incremental false` **exit 0**, all 9 files confirmed in `--listFiles`. No schema change, no migration, no SQL — **nothing for the user to apply.**
  - **As-built:** `TenantSchemas.cs` (3 DTOs) · `GetTenants.cs` + `GetTenantById.cs` under `OpsBusiness/Tenants/Queries/` · `EndPoints/Ops/Queries/TenantQueries.cs`, both gated `PLATFORM_TENANTS`/`PLATFORM_TENANT_VIEW`. FE: `TenantDto.ts`, `TenantQuery.ts`, `tenant-list-page.tsx`, `tenant-detail-page.tsx`, `tenant-status-chip.tsx`, tenants barrel, `ops/index.ts` edit, the two route pages.
  - **Redirect confirmed removed** — `(master)/ops/tenants/page.tsx` now renders `<TenantListPage />` only; `[companyId]/` coexists with the static `provisioning-runs/` segment.
  - **Verified names (the traps):** DbSets `Companies`/`Subscriptions`/`Plans`; **Plan display field is `PlanName`, not `Name`**; audit field `CreatedDate`; `IsInternal`/`IsDeleted` are `bool?` → compared `!= true`. `$advancedFilter` shipped as **`QueryBuilderModelInput`** (wired but passed `undefined` — no filter UI yet).
  - **Decision — "current subscription" rule:** prefer `Status == "Active"`; if none, take the newest by `StartDate` desc. Single object, never a collection. List enrichment is bounded to the current page's company ids.
  - **Open for PM (hand-back §⑨):** `Company.Status` vocabulary is still not formally enumerated — the chip maps both the company and subscription vocabularies with a visible raw-value fallback; confirm the canonical set. Also: `SourceLeadId` renders as a bare number (no lead-detail route yet), and amounts format from `currencyCode` (ISO), not a symbol.
- **Why it exists (in-session origin):** P-04 shipped the seeded `PLATFORM_TENANTS` menu pointing at `/ops/tenants`, but the tenant list itself was explicitly later-prompt scope (P-04 §2 item 5) — so a temporary FE **redirect** page forwarded `/ops/tenants` → `/ops/tenants/provisioning-runs`. This session also fixed two adjacent issues while getting the platform surface to render: (a) the `(master)` route group had **no shell** (bare `layout.tsx`, no `DashBoardLayoutProvider`) so the PLATFORM menus never appeared — fixed by nested `(master)/platform/layout.tsx` + `(master)/ops/layout.tsx` shells; (b) `TenantProvisioningQuery.ts` typed `$advancedFilter` as `String`, which HotChocolate rejected at validation (`QueryBuilderModelInput` expected) — fixed. P-07 replaces the redirect with the real screen and bakes both traps in as hard constraints.
- **Scope (task T-A13, read-only MVP):**
  1. **BE:** `GetTenants` (paginated list) + `GetTenantById` (profile + current subscription) under `OpsBusiness/Tenants/Queries/`; new `TenantQueries : IQueries` endpoint. Both control-plane (`IgnoreQueryFilters()` + `IsDeleted != true`), gated `PLATFORM_TENANT_VIEW`, excluding `IsInternal` companies. Plan/subscription resolved via **one bounded per-page lookup** into `Subscriptions`.`Include(Plan)` (mirrors the P-04 monitor enrichment). No mutation, no new command.
  2. **FE:** the list screen **replaces** `(master)/ops/tenants/page.tsx` (the redirect); read-only detail at `(master)/ops/tenants/[companyId]/page.tsx` (coexists with the static `provisioning-runs/` segment); header **"Provisioning Runs"** action → the P-04 O-03 monitor (the list→monitor navigation the user asked for). Reuses `usePlatformCapabilities` for `PLATFORM_TENANT_VIEW`.
- **Grounded against real source before authoring:** `Company.cs` (int `CompanyId`, `IsInternal`, `OnboardedOn`, `Subdomain`/`CustomDomain`, `Status`, `Country` nav), `Subscription.cs` (`PlanId`, `Status`, `Amount`, `BillingCycle`, `CurrencyId`, `Plan` nav), the P-04 `GetProvisioningRuns.cs` control-plane grid handler + `TenantProvisioningQueries.cs` endpoint, the seeded `PLATFORM_TENANT_VIEW` capability, and P-04's `TenantProvisioningQuery.ts`/`usePlatformCapabilities` FE anchors.
- **Key correctness rules baked in:** target `net10.0`; **no schema change / no migration** (read-only over existing tables — if a column looks missing, produce a spec instead); every read `IgnoreQueryFilters()` + explicit `IsDeleted != true` (platform null-tenant context); exclude `IsInternal != true`; **FE `$advancedFilter` MUST be `QueryBuilderModelInput`, copied from a *working* grid query — explicitly NOT from `TenantProvisioningQuery.ts`** (its known-bad line); pages render body-only (the `(master)/ops` shell already supplies Header + Sidebar); verify DbSet names + `Plan` display field before use.
- **Out of scope (fast-follow):** suspend/reactivate (`PLATFORM_TENANT_SUSPEND`), plan change (`PLATFORM_PLAN_EDIT`), entitlement editing, impersonation; the per-tenant "this tenant's runs" deep-link (blocked — `GridFeatureRequest` has no server-side company predicate, P-04 §2 item 3, so the header links to the global monitor); any migration/new capability/menu re-seed.
- **Expected hand-back (`PSS-2.0-ONBOARDING-P07-HANDBACK.md`):** BE build 0 err (user builds BE) + FE `tsc` exit 0; files created/edited; the DbSet + `Plan` display-field names verified; the "current subscription" selection rule chosen; the exact `advancedFilter` type shipped; confirmation the redirect page was replaced; any property/route name that differed.

---

## Sequencing decision — 2026-07-28

**User direction:** test the **assisted (staff-driven) lead flow first**, then add a **public product-page lead-capture** as new scope, then continue those flows.

1. **✅ DB apply DONE — 2026-07-28 (user).** All 3 migrations (M1/M2/M3, P-02b folded into M2) **authored + run** and all 6 seeds **applied**, per `PSS-2.0-ONBOARDING-MIGRATION-AND-SEED-RUNBOOK.md`. Schema + platform RBAC + billing catalog/prices + lead/deal settings are live in the database. **Remaining before the smoke test can pass:**
   - ⬜ **Populate `__TEMPLATE__`** through the app UI (roles, master data, settings, number sequences) — runbook §3. Skip this and every provisioned tenant comes out empty; it is *not* covered by the SQL seeds (S1 seeds only the shell).
   - ⬜ **Set `PLATFORM_ACTIVATION_URL_TEMPLATE.CurrentValue`** to the real host pattern before any run that sends a live activation email.
   - ⬜ **Run the end-to-end smoke test** (runbook §4): S-01 Lead → S-02 deal (discount >15% to exercise approval) → mark WON → O-01 wizard → provisioning run → tenant appears with roles/data cloned from `__TEMPLATE__` → `TENANT_ADMIN_ACTIVATION` email. **Time it → settles D-Q8.**
   - **Phase 0 DNS is only needed for the *final* hop** (prospect opening their tenant subdomain via the activation link). Steps 1–7 are testable *before* DNS is stood up.
2. **Later — public lead-capture product page = NEW scope (now P-08).** A public EXTERNAL_PAGE-style page feeding `ops.Lead` (self-service was explicitly OUT of P-01–P-06). To be generated once the assisted flow is verified. *(Renumbered from P-07 → P-08 on 2026-07-28: P-07 was taken by the O-05 Tenant List, which arose as an in-session control-plane deliverable — see the P-07 log entry below.)*

---

## Open blocking decisions (mirror of task-list top)

| # | Decision | Blocks | Status |
|---|----------|--------|--------|
| D-Q4 | Which modules ship in which plan? | P-02 seed + provisioning step 4 | ✅ **DECIDED 2026-07-24** — matrix in `PSS-2.0-ONBOARDING-DQ4-MODULE-PLAN-MAP.md` (FREE=Contacts+Donation+capped-Email · Case&Grant=100K-only · Event/Volunteer/Membership=50K) |
| D-Q7 | Who owns onboarding (sales/impl/support)? | P-03 `PLATFORM_*` roles | ✅ **DECIDED 2026-07-24** — 5 roles (Sales/Impl/Support/Finance/Admin) · provision trigger = Impl + Admin · P-03 seeds capabilities + role bundles. Matrix in `PSS-2.0-ONBOARDING-DQ7-PLATFORM-ROLES-MAP.md` |
| D-Q9 | How is a plan priced across currencies + do we model gateways now? | P-02b + P-03 step 2 | ✅ **DECIDED 2026-07-24** — **price book (`billing.PlanPrice`) + FX fallback** (curated price wins; else convert base via `IFxRateService` direct-pair; fail-closed if no rate). Subscription always snapshots final Currency+Amount. **Defer gateway** — capture `PaymentGatewayCode` string now, integrate later. Built in P-02b. **🔧 Refined same day:** catalog currency (`Plan` + `PlanPrice`) = **`CurrencyId` FK → `com.Currencies`** (not ISO string); `Subscription.Currency` stays ISO snapshot VALUE. |
| D-Q8 | Target time-to-live-tenant (rec. < 1 hr) | defines "done" for the engine | ⬜ open |
| D-Q5 | Discount threshold needing approval | P-05 approval routing | 🟡 **resolved-by-config 2026-07-27** — threshold = platform setting `PLATFORM_DISCOUNT_APPROVAL_THRESHOLD_PCT`, **default 15%**, tunable without code change (approval mechanism built regardless). Confirm/override the default. |

> **D-Q4 is the one to clear before P-02.** Everything downstream of provisioning step 4 waits on it.
