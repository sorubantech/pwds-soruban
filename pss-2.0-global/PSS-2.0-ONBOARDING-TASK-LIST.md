# PSS 2.0 — Company Onboarding: Build Task List

**Scope:** the onboarding process only — from a **WON lead** to an **ACTIVE tenant whose primary admin has logged in**. Lead capture and the marketing site are the GTM half and are tracked separately; they appear here only where onboarding depends on them.

**Source of truth:** `PSS-2.0-GTM-LEAD-ONBOARDING-AND-PRODUCT-ADMIN-APPROACH.md` (engine §6.4/§9, sequences §6.6, screens §5.4/§5.5, API §8.4, MVP §12) and `PSS-2.0-PLANS-AND-ENTITLEMENTS-APPROACH.md` (billing skeleton).

**Conventions carried in from project rules**
- 🔒 **Migrations are user-owned.** For any schema task I write the entity + EF config + a migration *spec*, and build to prove it compiles. **The user authors, runs, and commits the migration.** Never `dotnet ef migrations add/update/remove`.
- 🌱 **Seed files:** I write them (idempotent `INSERT … WHERE NOT EXISTS`, `sql-scripts-dyanmic` style); the user applies them.
- All `DateTime` columns `timestamptz`, values `DateTimeKind.Utc`. Audit fields `createdDate`/`modifiedDate`.
- All PKs/FKs `int` identity. `Company.CompanyId` is `int`.
- Register every new screen in `.claude/screen-tracker/REGISTRY.md` before `/plan-screens`.

**Layer legend:** `INFRA` · `BE` (.NET) · `FE` (Next.js) · `DB` (🔒 migration) · `SEED` (🌱) · `DECISION` (management, blocks a task).

---

## Blocking decisions (clear these first — each blocks a task below)

| # | Decision (from §14) | Blocks |
|---|---|---|
| D-Q4 | ~~**Which modules ship in which plan?**~~ **RESOLVED by T-A17** — the Plan Catalog screen makes the entitlement matrix SUPERADMIN-editable, so the mapping is now a refinable **seed default**, not a hard blocker. T-A5 seed sets sane defaults; step 4 reads whatever the catalog holds. | ~~T-A5 seed, step 4~~ (no longer blocking) |
| D-Q7 | Who owns onboarding — sales / implementation team / support? | `PLATFORM_*` role design (T-A9) |
| D-Q8 | Target time-to-live-tenant (recommend **< 1 hour** from WON) | Defines "done" for the whole engine |
| D-Q5 | Discount threshold needing management approval (e.g. >15%)? | Commercial approval routing (T-B4) |

---

## Phase 0 — Domain & infra prerequisite  *(size S, external lead time — start day one)*

> Nothing is reachable without this, and it is procurement/DNS lead time, not code. Start immediately, finish before Phase A lands.

| ID | Task | Layer | Depends on | Done when |
|----|------|-------|-----------|-----------|
| T-0.1 | Register/confirm apex domain; carve the three hostnames `www` · `admin` · `*.<product>.com` | INFRA | — | DNS zone exists |
| T-0.2 | **Wildcard DNS** `*.<product>.com` → app ingress | INFRA | T-0.1 | `anything.<product>.com` resolves |
| T-0.3 | **Wildcard TLS cert** via **DNS-01 challenge** + auto-renew | INFRA | T-0.2 | `https://x.<product>.com` serves valid cert with no per-tenant step |
| T-0.4 | Bind three host headers to one deploy: `admin`→`(master)` route group, `www`→`(public)`, `*`→tenant app | INFRA | T-0.3 | Each host routes correctly; tenant resolved from subdomain |
| T-0.5 | Reserved-subdomain blocklist as config (`www, admin, api, app, mail, static, cdn, status, support, blog, login, auth` + profanity) | BE | — | Blocklist reachable by the availability check (T-A7) |

---

## Phase A — Provisioning engine  *(size L — the hard, high-value core; do this before any wizard UI)*

### A.1 Data model

| ID | Task | Layer | Depends on | Done when |
|----|------|-------|-----------|-----------|
| T-A1 | `ops.TenantProvisioningRun` (Id, LeadId, CompanyId?, IdempotencyKey UNIQUE, Status RUNNING/SUCCEEDED/FAILED, current step, error, audit) + `ops.TenantProvisioningRunStep` (RunId, StepNo, StepName, Status, startedOn, finishedOn, error) | DB 🔒 | — | Entities + EF config compile; **migration spec handed to user** |
| T-A2 | `app.Companies` additions: `Status` (PROVISIONING/ACTIVE/SUSPENDED), `IsInternal bool`, `OnboardedOn timestamptz?` | DB 🔒 | — | Compiles; spec handed to user; existing rows default sanely |
| T-A3 | **Template company** row (`IsInternal=true`, `CompanyCode='__TEMPLATE__'`) as the clone source for roles/master-data/settings/fields | SEED 🌱 | T-A2 | Idempotent seed written; user applies; template is complete + inert |

### A.2 Plans skeleton (dependency of step 4 — build alongside, not after)

| ID | Task | Layer | Depends on | Done when |
|----|------|-------|-----------|-----------|
| T-A4 | ✅ **DONE** — `billing` entities: `Plan`, `PlanEntitlement`, `PlanQuota`, `PlanPrice`, `Subscription` (+ `CommercialTermId` FK), `SubscriptionOverride`, `UsageCounter` — all `int` keys, migrated | DB 🔒 | — | Compiles; migration spec handed to user |
| T-A5 | ✅ **DONE** — `IEntitlementService` + `IPlanPricingService` + idempotent 4-plan catalog/prices/backfill seed **from D-Q4 defaults** | BE + SEED 🌱 | T-A4, **D-Q4** | Given a plan, returns the capability ∩ entitlement set that step 4 consumes |

### A.2b Plans P2 — screens + enforcement (the plan screen + plan-based permission & data restriction)

| ID | Task | Layer | Depends on | Done when |
|----|------|-------|-----------|-----------|
| T-A17 | ✅ **BUILT (2026-07-29)** — code complete; **BE `dotnet build` deferred to the user**, and the `TrialDurationDays` migration + `billing-plan-trial-duration-seed.sql` + `ops-platform-plan-view-capability-seed.sql` are **user-owned and unapplied**. FE `tsc` exits 0. See PROMPT-11 §⑬ for the 7 carried follow-ups (incl. the deliberate recurring-plan trial-date deviation from §②b). — **Plan Catalog + Subscription assignment + `myEntitlements`** (`PROMPT-11-PLAN-SCREENS`) — the "plan screen". SUPERADMIN CRUD over `Plan` + `PlanEntitlement` feature matrix + `PlanQuota` limits + `PlanPrice` price book on `(master)` `/ops/plans` (gated `PLATFORM_PLANS`/`PLATFORM_PLAN_EDIT`, diff-only saves per band); Subscription assign/override/status on the tenant hub (price snapshotted via `IPlanPricingService`, filtered-unique-safe transition); real tenant-facing **`getMyEntitlements`** query (resolved feature/limit map + live usage — STOCK via `COUNT(*)`, FLOW via `UsageCounter`; fail-closed `Status="None"`). **Resolves D-Q4** (matrix now editable, seed = defaults only). **Free-plan term (§②b):** Free plan is time-boxed (7/14/30d, not lifetime) → **one additive nullable column `Plan.TrialDurationDays`** (user-owned migration + Free seed default), term control on the catalog screen, Trial/`TrialEndsOn` computed at assignment, trial fields on `getMyEntitlements`, + a **fail-closed trial-expiry check** in `EntitlementService.ResolveAsync`. Two `IEntitlementService` additions: `InvalidateAll()` (catalog-wide cache bust) + the expiry check. Supersedes the hardcoded `GetCompanySubscriptionInfo` placeholder (#75 §8, re-point deferred). **One nullable column only** (migration + seed patch user-owned); no new entity, no other schema change. | BE + FE | T-A4, T-A5, P-02b | A SUPERADMIN edits the plan matrix on-screen, assigns a tenant a plan with a price snapshot, and a tenant reads its live entitlements + usage — no seed SQL |
| T-A18 | **Entitlement & quota enforcement — soft-block** (`PROMPT-12-ENTITLEMENT-ENFORCEMENT`) — plan-based permission + data-level restriction. Two MediatR behaviors inserted **after `TenantAccessBehavior`, before `CommandValidationBehavior`**: `FeatureEntitlementBehavior` + `[RequiresFeature("CHANNEL:…")]` (403 `PLAN_FEATURE_NOT_ENTITLED`) and `QuotaBehavior` + `[MeteredResource(meter,type)]` (402 `PLAN_QUOTA_EXCEEDED`). **Soft-block policy (user-confirmed):** warn 80% / block **new creates** at 100% / existing data stays **read+edit** / downgrade-below-usage = read-only-over-limit, never delete. First gates: CONTACTS + DONATIONS stock (on `CreateContactCommand` + global-donation create, TOCTOU `pg_advisory_xact_lock`), EMAIL/WHATSAPP/SMS channel sends (system/transactional mail NOT gated), menu/module hiding by `TenantEntitlements.Features`. FE: usage meters + 80%/100% upgrade CTAs (consumes `getMyEntitlements`) + 402/403 upgrade dialogs. SuperAdmin/background bypass. **No schema change, no migration, no new seed** (writes existing `UsageCounter` for FLOW). | BE + FE | **T-A17** | A Free tenant is blocked from the 50,001st contact (existing stay editable), a not-entitled channel send 403s, and off-plan modules vanish from the nav — server-enforced |
| T-A19 | ✅ **BUILT (2026-07-30)** — code complete, BE `dotnet build` 0 errors, FE `tsc` exit 0; **migration + both seeds user-owned and UNAPPLIED**. — **Billing foundation — everything up to but not including taking a card** (`PROMPT-13-BILLING-FOUNDATION`, derived from `SELF-SERVE-UPGRADE-AND-BILLING-APPROACH`). Invoice ledger (`billing.Invoices` + `InvoiceLines` + `SubscriptionPayments`), `IPlanPricingService` **price ladder FREE→BOOK→FX→null** (fail-closed, direct-pair FX only, uplift + upward-only rounding), `SelfServeEvaluator` with a fixed reason precedence (`PLATFORM_DISABLED` → `TENANT_DISABLED` → `CUSTOM_PLAN` → `NO_PRICE_FOR_CURRENCY`), platform pricing levers + **tri-state** `Companies.AllowSelfServeUpgrade` (null = follow platform default, never coalesced), and hand-keyed **offline payment** recording (GATEWAY method refused — gateway money is written by its own callback). Tenant surface: `/billing` (status + trial/past-due banner that names the consequence **and its date** + last-3-invoice strip + `PlanUsagePanel` rehosted unchanged), `/billing/plans` (new tenant-gated `mySellablePlans` — **one** quote per plan in the tenant's own currency, never the operator's price book), `/billing/invoices` (read-only; no PDF until the gateway task). **Every actionable card reads "Contact us" from `canSelfServe`/`selfServeBlockedReason`, not from hardcoded JSX — the gateway task (`PROMPT-14-BILLING-GATEWAY`) flips behaviour by making the predicate pass.** Money is always a SNAPSHOT (amount, currency, FX rate value, plan code) — never an FK to a mutable price row, never re-derived. Schema: 3 cols on `billing.Subscriptions`, 1 on `app.Companies`, 3 new tables. | BE + FE | **T-A17, T-A18** | An operator records a bank transfer against a tenant and the invoice settles; the tenant sees their plan, price, FX derivation and invoice history — and is told *why* they can't self-upgrade, in words, from the server |
| T-A20 | 🟡 **BE + FE BUILT (2026-07-31), NOT YET CHARGEABLE** — §③–§⑥ and §⑧ code complete (platform encryption overload, 4 new entities + 12 columns, fail-closed `IPlatformGatewayResolver`, `IPlatformBillingPaymentService`, checkout/confirm/auto-renew commands, the 4-pass renewal + dunning cron at 03:00 UTC, platform webhook with duplicate/repair handling); **§⑦ FE now built** in a second session — `/billing/checkout`, plans cards routing on `canSelfServe`, auto-renew toggle, past-due banner, ops `/platform/gateways` (write-only credentials, blank = unchanged) + `/platform/webhook-logs`, the production-confirm environment switch, and per-tenant gateway activity on the tenant hub; FE `tsc` exit 0, no backend file touched. **Three things still block a real charge:** the migration + `billing-gateway-platform-seed.sql` are user-owned and UNAPPLIED (every new screen throws `relation does not exist` until then); **no payment-method API exists**, so the payment-method card could not be built; and no operation has been executed against a running API (the machine ran out of paging file mid-`dotnet run`). Five billing email templates still unseeded. See PROMPT-14 §⑬ Known Issues 5–15. — **Billing gateway — taking the card** (`PROMPT-14-BILLING-GATEWAY`). The half PROMPT-13 deferred: flips `canSelfServe` from "Contact us" to a real charge. **§⓪ decision: we own the billing schedule, the gateway does not** — the three providers' recurring models are incompatible (Braintree = true gateway subscription; Razorpay = needs a gateway-side Plan object, `RetrySubscriptionCharge`/`UpdateSubscriptionAmount` both return `Success=false`; PayU = no subscription object at all, SI mandate + merchant-initiated debit per cycle). `ops.PlatformPaymentGateways` (**no `CompanyId`. Ever** — platform billing is the opposite direction of money from `fund.CompanyPaymentGateways`) + `ops.PlatformGatewayCurrencies` (**fail-CLOSED**) + `IPlatformGatewayResolver` + new `NO_GATEWAY_FOR_CURRENCY` self-serve reason. `IEncryptionService` gains `EncryptForPlatform`/`DecryptForPlatform` (HKDF info `"platform-billing"`; **never `companyId: 0`**). Platform webhook at `POST api/webhooks/platform/{gatewayCode}` — no tenant segment, tenant resolved from the payload. FE: `/billing/checkout` (nonce only, never our own card field), payment-method card, auto-renew toggle, past-due banner naming the date; ops platform-gateway config (**write-only credentials**) + platform webhook log viewer. Plan change delegates to the existing `AssignSubscription`, not a reimplementation. | BE + FE | **T-A19** | A tenant self-upgrades with a real card, the webhook settles the invoice, and a declined renewal dunning-retries and suspends on schedule — no operator touch |
| T-A21 | ✅ **BUILT (2026-07-30)** — code complete, FE `tsc` exit 0; **BE `dotnet build`, the migration (2 cols on `com.PaymentGateways` + `com.PaymentGatewayCurrencies`) and both seeds (`paymentgateway-capability-seed.sql`, `paymentgateway-menu-hide-seed.sql`) are user-owned and UNAPPLIED**; Braintree's real enabled-currency list still outstanding (placeholder seeded). See PROMPT-15 §⑬. — **Tenant payment-gateway configuration** (`PROMPT-15-TENANT-GATEWAY-CONFIG`). Completes REGISTRY screen **#167**, absorbs **#168**. **Fixes a live bug (§⓪): the factory implements BRAINTREE/RAZORPAY/PAYU while the seed ships STRIPE/PAYPAL/RAZORPAY/BRAINTREE/SQUARE/MANUAL** — PAYU is unselectable, and three selectable gateways accept real merchant credentials then throw `NotSupportedException` at the donor's checkout. `com.PaymentGateways` gains `IsImplemented`/`SupportsRecurring`; new `com.PaymentGatewayCurrencies` (+ `RequiresActivation`) **fails OPEN** — deliberately opposite to T-A20's `ops.PlatformGatewayCurrencies`, different blast radius. Hardcoded `ValidateRazorpayCurrencies` hard-throw **deleted from both handlers**, replaced by warn-only `gatewayRecommendations` (`Recommended`/`Usable`/`Mismatch`/`Unknown`) + a guard that refuses only zero-currency-intersection — **the rare multi-currency charity must still be able to save a Mismatch**. Master `createPaymentGateway`/`deletePaymentGateway` removed outright; `PAYMENTGATEWAY` menu hidden (`IsLeastMenu=false`,`IsActive=false`) not deleted; both byte-identical duplicate master surfaces retired. 2-tab CONFIG at the **existing** `setting/paymentconfig/companypaymentgateway` URL (REGISTRY's rename declined — it orphans the seeded `MenuUrl`). Finishes the four stubs: the dead `onEdit`/`onToggle` no-ops, row-specific Test Connection (**new `TestGatewayConnectionAsync` — the existing `GenerateClientTokenAsync` tests the DEFAULT gateway and would go green on a broken row**), webhook log viewer; the two hardcoded fake rows are **deleted, not faked better**. | BE + FE | **none** (independent of T-A20 — `com.*`/`fund.*` vs `ops.*`; only `PaymentFlowService.cs` is shared, and T-A21 owns it. **EF migrations must still be sequenced** — one `ModelSnapshot`) | An Indian tenant sees Razorpay + PayU recommended and Braintree flagged-but-still-savable for its USD stream; no unimplemented gateway is selectable; Edit/Toggle/Test/Logs all do real work |
| T-A22 | ✅ **BUILT (2026-08-03)** — FE `tsc` exit 0; **BE `dotnet build`, the migration (§3.6: `ops.Leads.Industry`…`ConsentIpAddress` + `billing.Plans.IsPubliclyListed NOT NULL DEFAULT false`) and both seeds (`plan-public-listing-seed.sql`, `product-enquiry-notification-seed.sql`) are user-owned and UNAPPLIED**. See PROMPT-20 §⑩. — **Product landing page + anonymous lead capture** (`PROMPT-20-PRODUCT-LANDING-PAGE`). The public front door: a Server-Component marketing page at `/{lang}/peopleserve` in a new `(public)/(marketing)` route group (**not** the locale root — `pwdatasolutions.com/{lang}` is the PWDS company site and PeopleServe is one product on it), whose enquiry form is **not a new form** — it is the anonymous entry to `ops.Leads`, which already exists end-to-end. **Creates no tenant, no user, no subscription and no trial**; one `Lead` with `Source = INBOUND` + `Status = NEW`, both server-forced, and `OwnerUserId`/`EstimatedPlanCode`/`LostReason`/`ConvertedCompanyId` unsettable from this surface. One anonymous mutation (`submitProductEnquiry`, rate-limited 5/min/IP) + two anonymous queries (`publicCountries` — purpose-built because `GetCountries` is **not** anonymous; `publicPlanTeasers` — `IsPubliclyListed` defaults **false** so nothing leaks by accident, and never exposes entitlement/quota/feature codes). The response is **opaque**: stored, duplicate-within-24h and honeypot-caught all return one shape, so the form is not an email-existence oracle. Copy centralised in one reviewable file; JSON-LD FAQPage generated from the same array the visible FAQ renders. **⚠️ CSRF + reCAPTCHA are `SERVICE_PLACEHOLDER` stubs (the verifier returns a hardcoded 1.0) — the rate limit and the `ReferralCode` honeypot are the only real spam defences at launch, on our first genuinely public un-slugged internet-facing form.** `PLATFORM_LEAD_NOTIFICATION_EMAIL` fails **quiet**: unset ⇒ no alert, submission still succeeds, leads pile up unseen on `/ops/leads` — set it to a real monitored inbox before applying the seed. | BE + FE + DB 🔒 + SEED 🌱 | T-A17 (plan catalogue), lead management | An anonymous visitor on `/en/peopleserve` reads real prices, submits the enquiry, and it lands on `/ops/leads` as a NEW inbound lead with consent stamped — no account created, no tenant touched |

### A.3 The command

| ID | Task | Layer | Depends on | Done when |
|----|------|-------|-----------|-----------|
| T-A6 | **`ProvisionTenantCommand`** — orchestrator: create Run, execute steps 1–9, park `FAILED` at the failing step, resume from failed step, never step 1 | BE | T-A1..T-A5 | Full green-path run creates a usable tenant; a forced mid-step failure parks + resumes correctly |
| — | ↳ Step 1 · create `app.Companies` (+ CompanyCode + **Subdomain uniqueness by index**) | BE | T-A2, T-A7 | idempotent |
| — | ↳ Step 2 · create `billing.Subscription` from approved `CommercialTerm` | BE | T-A4, T-B3 | idempotent |
| — | ↳ Step 3 · seed Roles from template (BUSINESSADMIN + standard set) | BE | T-A3 | idempotent |
| — | ↳ Step 4 · seed RoleCapability/RoleModule **filtered by plan entitlements** | BE | T-A5 | idempotent; capabilities = template ∩ plan |
| — | ↳ Step 5 · seed MasterData + MasterDataType from template | BE | T-A3 | idempotent |
| — | ↳ Step 6 · seed OrganizationSetting defaults + NumberSequence definitions | BE | T-A3 | idempotent |
| — | ↳ Step 7 · seed Field/GridField per-tenant config | BE | T-A3 | idempotent |
| — | ↳ Step 8 · create primary admin `User` (`PENDING`) + UserRole BUSINESSADMIN + **one-time activation token** (no password) | BE | T-A2 | idempotent; token single-use + expiring |
| — | ↳ Step 9 · send welcome email + stamp `Lead.ConvertedCompanyId`/`ConvertedOn` (set once) | BE | T-A8 | idempotent; Lead→CUSTOMER |
| T-A7 | **Subdomain availability + validation** service — lowercase alnum+hyphen, 3–63 chars, unique index, reserved blocklist, immutable once ACTIVE | BE | T-0.5, T-A2 | `check(subdomain)` returns available/taken/reserved/invalid |
| T-A8 | **Activation-token flow** — issue single-use expiring token (step 8), `POST /activate` sets the admin's own password on the tenant domain, marks User `ACTIVE` | BE + FE | T-A6 | Admin sets password on `<tenant>` host; token burns on use; **no password ever emailed** |
| T-A9 | `PLATFORM_*` capability family + `ProvisionTenant` authorization (NOT `IsSuperAdmin()` alone) | BE | **D-Q7** | Only platform staff can invoke provisioning; enforced in the pipeline |
| T-A10 | **Abandon-provisioning** cleanup path — DELETE-cascade allowed only for runs that never reached step 8 (no user has logged in); after step 8 → suspend, never delete | BE | T-A6 | Half-run before step 8 is cleanly removable; post-step-8 blocked |

### A.4 Provisioning API + monitor UI

| ID | Task | Layer | Depends on | Done when |
|----|------|-------|-----------|-----------|
| T-A11 | GraphQL surface (§8.4): `provisionTenant`, `resumeProvisioning`, `abandonProvisioning`, `checkSubdomain`; queries for runs/steps | BE | T-A6, T-A7, T-A10 | Wired; `PLATFORM_*` guarded |
| T-A12 | **O-03 Provisioning Run Monitor** — `MASTER_GRID` of runs + step-by-step detail (status, duration, error) + Resume/Abandon actions | FE | T-A11 | Support can see *which step failed* and resume without reading logs |
| T-A13 | **O-05 Tenant List + read-only Tenant Detail** — `/ops/tenants` control-plane list of provisioned tenants (name/subdomain/plan/status/onboarded), header nav → O-03 run monitor; read-only detail at `/ops/tenants/[companyId]` (profile + current subscription). Read-only MVP — **no** suspend/reactivate/plan-change (fast-follow). Gated `PLATFORM_TENANT_VIEW`; control-plane null-tenant reads (`IgnoreQueryFilters()` + `IsDeleted != true`); excludes `IsInternal`. No schema change. | BE + FE | T-A11, T-A12 | A platform operator sees every real tenant, opens one, reads its plan/subscription, and jumps to its provisioning history — no SQL |
| T-A14 | **Platform communication providers — org/PLATFORM-level sender config** (`PROMPT-08-PLATFORM-COMMS-PROVIDERS`) — new `ops.PlatformCommunicationProviders` (channel-scoped EMAIL/SMS/WHATSAPP, no `CompanyId`; entity+config+DbSet+migration spec+idempotent seed **built** last session, migration+seed **user-owned/pending apply**). **Root-caused the silent no-mail:** the platform welcome path `EmailTemplateService.SendEmailByTemplateKeyAsync` sent only via the global appsettings SendGrid key and **Step 9 discarded its `bool`** → run went green with no mail. P-08 scope: (A) Step 9 (`SEND_WELCOME`) **honors the send result** — non-fatal but observable warning on failure, mail actually sends once the provider row is seeded; (B) extract reusable `IPlatformCommunicationProviderResolver` (behaviour-preserving EMAIL refactor) + `IPlatformCommunicationService` routing SMS/WhatsApp through the **existing** provider factories by `Channel`. Reuses `EmailProviderFactory`/`ISmsProviderFactory`/WhatsApp sender; `ProviderConfiguration` plaintext JSON. **No engine change beyond the Step-9 caller edit; no schema change (table pre-built).** | DB 🔒 (built) + BE + SEED 🌱 | T-A6 step 9, T-B6 | Provisioning welcome email actually delivers via the seeded platform provider; Step 9 records the true send outcome; all platform-owned comms resolve their sender from `ops.PlatformCommunicationProviders` |
| T-A15 | **Platform comms — control-plane CRUD + `(master)` management screen** (`PROMPT-09`, follow-up to T-A14) — GraphQL list/getById/create/update/delete + set-default + test-send for `ops.PlatformCommunicationProviders`, gated by a `PLATFORM_*` capability, plus a `(master)` CONFIG screen so operators manage providers without editing seed SQL. Sensitive config (API keys) masked + write-only + audited. **Deferred out of P-08.** | BE + FE | T-A14 | An operator adds/edits/defaults a platform EMAIL/SMS/WhatsApp provider and test-sends, no SQL |
| T-A16 | **Resend tenant activation link** (`PROMPT-10-RESEND-ACTIVATION`, fast-follow to T-A13) — closes the stranded-tenant gap: if the welcome mail never arrived (or the 72h token expired), a platform operator can re-send the passwordless activation link from the `(master)` tenant hub. New `ResendTenantActivationCommand(companyId)` gated `PLATFORM_TENANTS` + `PLATFORM_TENANT_PROVISION`; **guarded to `Status=='PROVISIONING'` only** (an `ACTIVE` admin uses Forgot Password); reuses/mints the 72h token (reuse if live, mint if expired). **Extract** `ITenantActivationService` (token-ensure + activation-URL + `TENANT_ADMIN_ACTIVATION` send) from `ProvisionTenant` Step8/9 so both share one path; `resendTenantActivation` mutation; button on `tenant-detail-page.tsx` (visible only while PROVISIONING). Non-fatal on mail failure (`EmailSent=false` warning). **No password ever generated/shown; masked email only. No schema change, no migration, no seed.** | BE + FE | T-A8, T-B6, T-A13, T-A14 | An operator resends the activation link to a stuck tenant from the hub; admin activates and the tenant flips ACTIVE — no SQL |

**Phase A exit test:** from a seeded WON lead + approved CommercialTerm, one `provisionTenant` call yields a reachable `acme.<product>.com`, admin sets their own password via the activation link, logs in as BUSINESSADMIN, and every provisioning step shows SUCCEEDED in O-03 — with no developer and no hand-run SQL.

---

## Phase B — Assisted wizard + commercial gate  *(size M — the MVP-1 default onboarding path)*

> The commercial gate must exist because provisioning step 2 has no input without an approved `CommercialTerm`.

| ID | Task | Layer | Depends on | Done when |
|----|------|-------|-----------|-----------|
| T-B1 | `ops.CommercialTerm` entity (plan, price, currency, discount, quota overrides, Status DRAFT/APPROVED, approver, audit) | DB 🔒 | T-A4 | Compiles; migration spec handed to user |
| T-B2 | **S-01 Capture commercial terms** (`FLOW`) on the won lead | FE + BE | T-B1 | Sales captures terms; saved DRAFT |
| T-B3 | **S-02 Approve** — approval routing (management sign-off above the D-Q5 discount threshold) → `APPROVED`, Lead → WON | FE + BE | T-B2, **D-Q5** | Terms cannot reach provisioning until APPROVED |
| T-B4 | **O-01 Onboarding Wizard (assisted)** — `FLOW`, 7 steps on `admin`: company info (pre-filled from Lead) → localization → **primary administrator identity (explicit)** → subdomain (last, permanent, live availability) → plan **read-only** → review → Provision | FE | T-A11, T-A12, T-B3 | Staff fill wizard from a won lead; "Provision" calls `provisionTenant`; primary admin never omitted |
| T-B5 | Wizard ↔ engine wiring: idempotency key from `LeadId`+`CompanyCode`; double-click "Provision" never makes two tenants; failed run surfaces the "we'll email you shortly" state, resumable from O-03 | FE + BE | T-B4, T-A6 | Double-submit safe; failure path visible + resumable |
| T-B6 | Welcome email template (tenant URL + activation link) via existing `EmailTemplate` infra, platform scope | BE + SEED 🌱 | T-A8 | The single assisted-mode email renders correctly; seed applied by user |
| T-B7 | **Lead lifecycle enforcement** (follow-up patch to P-05, `PROMPT-05B`) — server-governed `Lead.Status`: ordered transition guard (`NEW→QUALIFIED→WON`, `→LOST`, `LOST→NEW` reopen), `WON` unreachable manually (derived from `ApproveCommercialTerm` only), create-time `WON` block; FE swaps the free status dropdown for lifecycle action buttons. **No schema / migration / capability / mutation.** | BE + FE | T-B2, T-B3 | `UpdateLead` rejects illegal transitions; `WON` set only on deal approval; FE dropdown removed |
| T-B8 | **Payment-gateway picker** (follow-up patch to P-05, `PROMPT-05C`) — replace the deal form's free-text `paymentGatewayCode` `FormInput` with a closed `FormSelect` (`RAZORPAY` / `STRIPE` + "— Not decided —"); field stays optional (blank → null). **FE only — no schema / migration / capability / mutation / seed.** Server-configurable gateway list deferred until a real gateway integration lands. | FE | T-B1 | Gateway is a dropdown of the two backend-named codes; blank saves `null`; edit pre-selects existing code |
| T-B9 | **Auto-approval master switch** (follow-up patch to P-05, `PROMPT-05D`) — new platform-global BOOLEAN setting `PLATFORM_DEAL_AUTO_APPROVE_ENABLED` (default `true`); gate the `SubmitCommercialTerm` `autoApproved` computation so **OFF ⇒ every deal → `PENDING_APPROVAL`** regardless of discount. Adds `LeadHelper.GetAutoApproveEnabledAsync` (reuses `GetPlatformSettingAsync`). **BE 2 edits + 1 seed row (`ops-deal-autoapprove-toggle-seed.sql`) — no schema / migration / capability / mutation / FE.** | BE | T-B1 | With setting `'false'` a 0%-discount deal lands PENDING; with `'true'`/absent, within-threshold still auto-approves |
| T-B10 | **Drop Document header/footer from O-01 wizard** (follow-up patch to P-05, `PROMPT-05E`) — remove the Step-3 `companyHeader` / `companyFooter` free-text fields from the provisioning wizard (schema + form + request payload). Redundant: `Company.CompanyHeader/Footer` are tenant-owned columns and receipts/pages/comms render from selected templates, not a parent-level string. **FE only, 2 files — no BE / schema / migration / capability / mutation / seed; `Company` columns retained; handler already defaults blank → company name.** | FE | T-B4 | Wizard Step 3 no longer shows Document header/footer; payload omits both keys; run still completes with `Company.*` = company name |
| T-B11 | **Provisioning: per-tenant Role unique indexes + resume-safe validator** (critical fix to P-03/P-04, `PROMPT-05F`) — Defect A: `RoleConfiguration` unique indexes `(RoleName,IsActive)`/`(RoleCode,IsActive)`/`(OrderBy,IsActive)` are **global** → Step 3 `SEED_ROLES` collides with the template's `BUSINESSADMIN`/OrderBy=1 role (`roles_isactive` violation), blocking every 2nd tenant; prepend `CompanyId` to all three. Defect B: `ProvisionTenantCommandValidator` rejects the resumable retry ("subdomain/code taken") → make both uniqueness `MustAsync` clauses exclude companies owned by this run's own idempotency key; extract `ProvisionIdempotency.KeyFor` shared helper. **BE: 1 config file (3 index edits) + validator + key helper; index-only migration is USER-OWNED (dev proves compile only); no rollback path (engine is resume-based); no new column/capability/mutation/query/seed/FE.** | BE | T-A6 | 2nd tenant provisions cleanly; re-submitting the stuck leadId=2 run resumes from Step 3 → SUCCEEDED, reusing the half-built company (no duplicate, no manual SQL) |

**Phase B exit test:** an implementation-team member takes a won, approved lead through O-01 in a few minutes, the tenant provisions, the customer receives exactly one welcome email, sets their own password, and lands in their tenant — the customer never touches `admin` and never holds a control-plane account (§6.6.2).

---

## Phase C — Go-live handoff  *(size S — closes the loop; can trail Phase B)*

| ID | Task | Layer | Depends on | Done when |
|----|------|-------|-----------|-----------|
| T-C1 | **O-04 / T-05 Go-Live checklist** (`CONFIG`/widget) on the tenant domain: branding · ≥1 user invited · gateway/email configured (if in plan) · contacts imported-or-skipped · one test donation | FE | Phase B | Checklist drives activation; visible to customer (± impersonation support) |
| T-C2 | Flip `Company.Status = ACTIVE` + stamp `OnboardedOn` on checklist completion (or on provision, per §6.5 policy) | BE | T-C1 | Lifecycle state correct; internal tenants excluded from metrics via `IsInternal` |

---

## Deferred to MVP-2 (self-service · Option A) — tracked, not built now

Gated on **D-Q1** ("do we sell self-service at all?"). Same engine (`ProvisionTenantCommand`), different shell:
- **O-02 / P-08** self-service wizard as a **token-authenticated public route** `www/onboarding/{token}` (token IS the auth — no account, no prospect login before the tenant exists).
- `ops.OnboardingInvite` + single-use invite token; "Send onboarding invite" action on the won lead.
- Email 1 (invitation link) + Email 2 (welcome/activation) — still no password in either.
- Plan/modules **read-only** to the customer.

---

## Critical path (one line)

**Phase 0 (start now, runs in parallel)** → T-A1/A2 → T-A4/A5 *(needs D-Q4)* → **T-A6 the command** → T-A11/A12 monitor → T-B1..B3 commercial gate *(needs D-Q5)* → **T-B4 wizard** → T-C1/C2 go-live.

Everything is blocked on **T-A6**, and **T-A6 step 4 is blocked on D-Q4**. Clear the module-to-plan mapping first.
